# Local development (not used on Render — use DATABASE_URL there)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hillradar_db
DB_USER=postgres
DB_PASSWORD=your_password

# Production (Supabase connection string — set this in Render env vars)
# DATABASE_URL=postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres

GOOGLE_MAPS_API_KEY=your_google_maps_key
PORT=3001
